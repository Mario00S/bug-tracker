pipeline {
    agent any
      tools { go '1.19' }

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir ('bugtracker-backend') {
                 sh 'go version'    
                 sh 'go test -v ./...'
                }
            }
        }
    }
}